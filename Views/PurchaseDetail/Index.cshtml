@model IEnumerable<prjGroceryStore4.purchaseDetail>

@{
    ViewBag.Title = "Index";
}

<h2>Purchases</h2>
@*@using (Html.BeginForm("Index", "PurchaseDetail", FormMethod.Get))
{
    @Html.TextBox("searching", null, htmlAttributes: new { @class = "form-control" })
    <input type="submit" class="btn btn-info" value="Search">
}*@
<table class="table table-striped">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.pd_quantity)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.pd_buyingPrice)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.purchase.p_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.product.pro_name)
            </th>
            <th></th>
        </tr>
    </thead>
    @*@if (Model.Count() == 0)
    {
        <tr>
            <td colspan="8">
                <div class="alert alert-danger">
                    <strong>No Match!</strong>
                </div>
            </td>
        </tr>
    }
    else
    {*@

       @foreach (var item in Model)
        {
            <tr>
                <td>
                    @Html.DisplayFor(modelItem => item.pd_quantity)
                </td>
                <td>
                   <b>$</b>@Html.DisplayFor(modelItem => item.pd_buyingPrice)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.purchase.p_id)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.product.pro_name)
                </td>
                <td>

                    @Html.ActionLink("Delete", "Delete", new { id = item.pd_id }, new { @class = "link-danger" })
                </td>
            </tr>
        }
    
    </table>
